{% comment %} 
  Alex - this is a global Modal Dialog (popup) lifted from other places on site
  Goal is to have an everpresent modal LIQUID for popping up quick messages
  - e.g. for loading "offer loading" popups
{% endcomment %}

<global-simple-modal
  id="GlobalModal"
  class="global-modal"
>
  <div
    role="dialog"
    aria-label="{{ 'products.product.choose_product_options' | t: product_name: card_product.title | escape }}"
    aria-modal="true"
    class="global-modal__content global-settings-popup"
    tabindex="-1"
  >
    <button
      id="GlobalModalClose"
      type="button"
      class="global-modal__toggle"
      aria-label="{{ 'accessibility.close' | t }}"
    >
      {{- 'icon-close.svg' | inline_asset_content -}}
    </button>
    <div
      id="GlobalModal-info"
      class="global-modal__content-info"
    >
      <div class="global-modal__content-switch">
        {% comment %} .always-visible just used to prevent > div hiding {% endcomment %}
        <div class="_pt-3 _pb-8 always-visible _flex _items-center _justify-center">
          {%- render 'loading-spinner' -%}
        </div>
        <div data-js-content="promo">
          <h1 class="_font-heading-primary md:_text-3xl _text-2.5xl _mb-3">Applying Gifts</h1>
          <p class="md:_text-lg _text-base">Please wait while we apply your gifts</p>
        </div>
        <div data-js-content="discount">
          <h1 class="_font-heading-primary md:_text-3xl _text-2.5xl _mb-3">Applying Discount</h1>
          <p class="md:_text-lg _text-base">Please wait while we apply your discount</p>
        </div>
        <div data-js-content="default">
          <h1>Loading…</h1>
          <p>Please wait a few moments</p>
        </div>
      </div>
    </div>
  </div>
</global-simple-modal>